# Backend Environment Variables
# Set these in your deployment platform (Render)

# Database Configuration
DATABASE_URL=postgresql://username:password@host:port/database

# Flask Configuration
FLASK_SECRET_KEY=your-super-secret-key-here

# CORS Configuration (comma-separated URLs)
ALLOWED_ORIGINS=https://your-frontend-vercel-url.vercel.app,http://localhost:3000

# Optional: Database initialization key
DB_INIT_KEY=your-random-key-here

# Optional: Upload size limit (bytes)
UPLOAD_MAX_SIZE=10485760